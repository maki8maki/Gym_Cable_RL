defaults:
  - fe: defaults
  - rl: defaults

memory_size: 10000
nsteps: 100
nepisodes: 5000
nevalepisodes: 5
update_every: 50
save_anim_num: 10
eval_num: 100
device: cuda
_replay_buffer:
  _target_: agents.utils.ReplayBuffer

basename: SAC_trainedDCAE_cl2

gathering_data: True

hydra:
  run:
    dir: logs/${basename}/${now:%Y%m%d-%H%M}
